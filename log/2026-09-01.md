
## 2026-09-01 (Week 5, Day 1)

**Drill:** Gaps-and-islands, cold redo (no notes, no AI). Failed Once.

- Attempt 3 (pass): bounded the spine to each seller's own first/last order month, fixed island grouping, dropped the hardcoded seller filter, added HAVING COUNT(*) >= 3.

- Open question to be able to explain cold: window ends at each seller's last order month, so a seller who went dark and never sold again shows no trailing zero-streak — worth naming this assumption explicitly in an interview.

**Infra issue found and fixed:** The dbt init olist_warehouse project from Week 1 Sat was sitting standalone at /Users/jay/olist_warehouse, never actually nested inside the Analytics-portfolio repo, meaning it was never really part of what got pushed for the W1 checkpoint. Also found log/ and drills/ (Week 1 Fri non-negotiables) were never created at all.

Fixed today:

- Merged olist_warehouse into Analytics-portfolio (clean merge, no nested .git).

- Found and deleted a stray decoy olist.db (empty, accidental typo of the real olist.duckdb).

- Python venv broke on the move (hardcoded absolute paths) — rebuilt fresh in place, reinstalled dbt-core + dbt-duckdb. dbt debug passes clean.

- Created log/ and drills/ today — explicitly not backdated to Week 1. The gap existed for 4 weeks; documenting it honestly rather than pretending otherwise.

**Checkpoints retroactively confirmed:**

- W2 (revenue reconciles to payment_value within 1%): pass.

- W3 (cohort table renders, month-0 = 100% every cohort, can explain ROWS vs RANGE cold): pass.

**What broke:** A whole session's worth of infra cleanup instead of forward progress. Not wasted — found and fixed a real gap before it became a Week 7 cold-clone-test failure.

**Tomorrow's first move:** Actually start the Week 5 Day 1 build — sources.yml for the 9 raw tables, freshness on orders.

